# Товары, работы, услуги

# Элемент чека (TicketRequest::Item)

| Поле | Наименование | Тип данных | Обязательно | Примечания |
| --- | --- | --- | --- | --- |
| type | Тип элемента | [TicketRequest::Item](Элемент%20чека.md)::<br>[ItemTypeEnum](Тип%20элемента%20списка%20товаров.md) | да |  |
| commodity | Предмет потребления | [TicketRequest::Item::Commodity](Предмет%20потребления.md) | Обязательно только если type == ITEM_TYPE_COMMODITY игнорируется в остальных случаях |  |
| storno_commodity | Сторно предмета потребления | [TicketRequest::Item](Элемент%20чека.md)::<br>[StornoCommodity](Сторно%20предмета%20потребления.md) | Обязательно только если type == ITEM_TYPE_STORNO_COMMODITY игнорируется в остальных случаях |  |
| markup | Наценка | [TicketRequest::Modifier](Модификатор%20стоимости.md) | Обязательно, только если type == ITEM_TYPE_MARKUP игнорируется в остальных случаях |  |
| storno_markup | Сторно наценки | [TicketRequest::Modifier](Модификатор%20стоимости.md) | Обязательно, только если type == ITEM_TYPE_STORNO_MARKUP игнорируется в остальных случаях |  |
| discount | Скидка | [TicketRequest::Modifier](Модификатор%20стоимости.md) | Обязательно, только если type == ITEM_TYPE_DISCOUNT<br>игнорируется в остальных случаях |  |
| storno_discount | Сторно скидки | [TicketRequest::Modifier](Модификатор%20стоимости.md) | Обязательно, только если type == ITEM_TYPE_STORNO_DISCOUNT игнорируется в остальных случаях |  |

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Item](Элемент%20чека.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Item::ItemTypeEnum](Тип%20элемента%20списка%20товаров.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Item::Commodity](Предмет%20потребления.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Tax](Налог%20в%20чеке.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Item::StornoCommodity](Сторно%20предмета%20потребления.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Modifier](Модификатор%20стоимости.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Payment](Оплата%20в%20чеке.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Payment::CardPaymentFields](Реквизиты%20оплаты%20картой.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Payment::MobilePaymentFields](Реквизиты%20мобильного%20платежа.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::Amounts](Итоги%20по%20чеку.md).

> [!NOTE]
> См. подробное описание структуры в файле [TicketRequest::ParentTicket](Родительский%20чек%20возврата.md).

### Спецификация на уровне Protobuf

Имя файла: [ticket.proto](../../../../proto/ticket.proto)

```protobuf
message Item {

    message Commodity {
      reserved 8;
      reserved "excise_stamp";

      optional uint64 code = 1; // uint32 с версии 200
      optional string name = 2;
      required string section_code = 3;
      required uint64 quantity = 4; // до версии 203 тип uint32
      required Money price = 5;
      required Money sum = 6;
      repeated Tax taxes = 7;
      repeated KeyValuePair auxiliary = 9; // добавлено в версии 200
      optional string physical_label = 10; // добавлено в версии 200
      optional string product_id = 11; // добавлено в версии 200
      optional string barcode = 12; // добавлено в версии 200
      required string measure_unit_code = 13; // добавлено в версии 201
      repeated string list_excise_stamp = 14; // добавлено в версии 203
      optional string ntin = 15; // добавлено в версии 203
    }

    message StornoCommodity {
      reserved 7;
      reserved "excise_stamp";

      optional string name = 1;
      required string section_code = 2;
      required uint64 quantity = 3; // до версии 203 тип uint32
      required Money price = 4;
      required Money sum = 5;
      repeated Tax taxes = 6;
      repeated KeyValuePair auxiliary = 8; // добавлено в версии 200
      optional string physical_label = 9; // добавлено в версии 200
      optional string product_id = 10; // добавлено в версии 200
      optional string barcode = 11; // добавлено в версии 200
      required string measure_unit_code = 12; // добавлено в версии 201
      repeated string list_excise_stamp = 13; // добавлено в версии 203
      optional string ntin = 14; // добавлено в версии 203
    }

    enum ItemTypeEnum {
      ITEM_TYPE_COMMODITY = 1;
      ITEM_TYPE_STORNO_COMMODITY = 2;
      ITEM_TYPE_MARKUP = 3;
      ITEM_TYPE_STORNO_MARKUP = 4;
      ITEM_TYPE_DISCOUNT = 5;
      ITEM_TYPE_STORNO_DISCOUNT = 6;
    }

    required ItemTypeEnum type = 1;
    optional Commodity commodity = 2; // required если type == ITEM_TYPE_COMMODITY
    optional StornoCommodity storno_commodity = 3; // required при type == ITEM_TYPE_STORNO_COMMODITY
    optional Modifier markup = 4; // required при type == ITEM_TYPE_MARKUP
    optional Modifier storno_markup = 5; // required при type == ITEM_TYPE_STORNO_MARKUP
    optional Modifier discount = 6; // required при type == ITEM_TYPE_DISCOUNT
    optional Modifier storno_discount = 7; // required при type == ITEM_TYPE_STORNO_DISCOUNT
  }
```
